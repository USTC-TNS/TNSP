language: cpp
compiler:
- clang
sudo: required
dist: xenial
cache:
  directories:
  - hptt/lib
before_install:
- sudo sh -c 'echo deb https://apt.repos.intel.com/mkl all main > /etc/apt/sources.list.d/intel-mkl.list'
- sudo apt-get update && sudo apt-get install intel-mkl-full_2019.2-187
- cd hptt && ls lib/libhptt.a lib/libhptt.so || make && cd ..
install:
- make compile
script:
- ./test.out
- ./Heisenberg_MPS_SU.out -L4 -D4
env:
  global:
    - MAKEFLAGS="-j 8"
